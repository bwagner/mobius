# present001
#
# Boss-Rec-Play-Overdub
#
!name Boss-Rec-Play-Overdub

if mode == reset
Record
end

else if mode == mute
Reset
Record
end

else if mode == record
Play
end

else if mode != reset
Overdub
end
endif
